//Create a function that takes two numbers
//as input and returns the sum of those numbers.
//call this functions with 2 and 3 arguments.

int add(int num1, int num2) {
  return num1 + num2;
}

void main() {
  int sum = add(2, 3);
  print('The sum is $sum');
}
